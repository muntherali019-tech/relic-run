name: Main App CI

# This repo is retired; the app lives in muntherali019-tech/Education-academy-.
# Automatic runs are off so a stale duplicate stops reporting green. Still
# runnable by hand from the Actions tab if you need to check out history.
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      # Gate on what actually ships. The remaining high advisories are all
      # transitive under @capacitor/cli (Android build tooling, devDependencies
      # only) and need a Capacitor major bump to clear; the full audit below
      # reports them without blocking so the debt stays visible.
      - name: Audit runtime dependencies (fail on high/critical)
        run: npm audit --omit=dev --audit-level=high

      - name: Audit all dependencies (report only)
        run: npm audit || true

      - name: Run tests
        run: npm test

      - name: Syntax-check server
        run: |
          for f in server/*.js; do
            echo "checking $f"
            node --check "$f"
          done

      - name: Build web
        run: npm run build:web

      - name: Build app (Capacitor bundle)
        run: npm run build:app

      - name: Build onefile
        run: npm run build:onefile

      - name: Smoke-test API (demo mode, no secrets)
        run: |
          PORT=8787 node server/index.js &
          SRV=$!
          for i in $(seq 1 20); do
            if curl -fsS http://localhost:8787/api/health > /dev/null; then
              echo "health OK"; break
            fi
            sleep 0.5
          done
          curl -fsS http://localhost:8787/api/health
          curl -fsS -X POST http://localhost:8787/api/auth/signup \
            -H 'content-type: application/json' \
            -d '{"email":"ci@example.com","password":"secret123"}' > /dev/null && echo "signup OK"
          kill $SRV 2>/dev/null || true
